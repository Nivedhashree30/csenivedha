#include<stdio.h>
int main()
{
int i; space;
char a[30];
print(Enter the string");
scanf("%s",a);
for(i=0;a[0]!="10";i++)
{
if(a[i]='')
{
 space++
 }
 }
 print("Number of space:%s", sp)
 print("Number of word in %", space +1);
 }
