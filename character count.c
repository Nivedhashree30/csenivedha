#include<stdio.h>
int main()
{
 char ch;
 unsigned in long line count;
 word count, char count;
 int 0;
 line count=0
 word count=0;
 char count=0;
 while()ch=getcc(std in);=EOF)
 {
 if (ch!='\n')
 {
 (++char count)
 }
 if(ch=='\n')
 {
 ++line count
 }
 if(char count>0)
 {
 ++word count;
 ++line count:
 }
 print("%\n %ln%ln ln"char count,word,line count);
 return 0;
 }
